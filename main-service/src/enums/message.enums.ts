export enum MessageType {
    TEXT = "text",
    IMAGE = "image",
    VIDEO = "video",
    AUDIO = "audio"
}

export enum MessageStatus {
    READ = "read",
    NOT_READ = "not_read",
    NOT_DELIVERED = "not_delivered"
}